<UserControl x:Class="Marvin.Products.UI.Interaction.Aspects.PropertiesAspectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Marvin.Controls;assembly=Marvin.Controls"
             xmlns:aspects="clr-namespace:Marvin.Products.UI.Interaction.Aspects"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance aspects:PropertiesAspectViewModel}">
    <d:DesignerProperties.DesignStyle>
        <Style TargetType="UserControl">
            <Setter Property="Background" Value="White"/>
        </Style>
    </d:DesignerProperties.DesignStyle>

    <controls:EntryEditor VerticalAlignment="Stretch"
                           HorizontalAlignment="Stretch"
                           BreadcrumbVisibility="Visible"
                           IsEditMode="{Binding IsEditMode}"
                           RootEntry="{Binding Product.Properties}"/>
</UserControl>
